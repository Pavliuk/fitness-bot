version: "3.9"

services:
  fitness-bot:
    build: .
    container_name: fitness-bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/app/data   # зберігає data/fitness.db поза контейнером
